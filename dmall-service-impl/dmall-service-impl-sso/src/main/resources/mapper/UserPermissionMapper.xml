<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dmall.sso.service.impl.admin.mapper.UserPermissionMapper">

    <select id="listByUserName" resultType="com.dmall.sso.service.impl.admin.dataobject.PermissionDO">
       select
               p.id, p.code, p.name
        from pms_permission p
	    left join bms_role_permission rp on p.id = rp.permission_id
	    left join bms_role r on r.id = rp.role_id
	    left join bmd_user_role ur on ur.role_id = r.id
	    left join bms_user u on u.id = ur.user_id
	    where u.username = #{userName}
    </select>

</mapper>
